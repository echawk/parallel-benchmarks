#!/bin/sh

MAX_PROCESSES="$(nproc)"

NUM_P=1
echo "hosts,time,num_processes,num_primes" > output.csv
while [ $NUM_P -le $MAX_PROCESSES ]; do
    NUM_PROCESSES=$NUM_P ./benchmark >> output.csv
    NUM_P=$((NUM_P + 1))
done
